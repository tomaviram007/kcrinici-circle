# הנחיות עבודה על הפרויקט

מסמך זה מרכז כללים שחוזרים על עצמם בפרויקט. `SYSTEM_PROMPT.md` מתאר את הארכיטקטורה,
כאן נמצאים הכללים שקל לפספס.

## תמונת רקע (Hero) לעמוד חדש

**כלל: תמונת ה-Hero חייבת להיות בנושא של העמוד עצמו, לא תמונה גנרית של המועדון.**

הנכסים ב-`src/assets/` הם ברובם תמונות של פנים המועדון (לאונג', כורסאות עור, סיגרים,
חדר ישיבות). קל ליפול למלכודת של לקחת את `hero-bg.jpg` כברירת מחדל לעמוד חדש, וזה
נראה מנותק. עמוד נדל"ן צריך בניינים, עמוד ספורט צריך ספורט, וכן הלאה.

לפני שמחברים עמוד חדש:

1. לבדוק אם קיים נכס ייעודי ב-`src/assets/` שבאמת מתאר את הנושא.
2. אם אין, לייצר תמונה חדשה בשפה הוויזואלית של האתר: כהה, זהב עמום (`#D4AF37`),
   אור חם, קונטרסט גבוה, אווירת מועדון פרטי. בלי טקסט, בלי לוגואים.
3. לשמור אותה כ-`src/assets/hero-<page>.jpg` ולייבא אותה כ-fallback.
4. להוסיף את מפתח העמוד ל-`PAGES` ב-`src/components/admin/AdminCovers.tsx`, כדי
   שאפשר יהיה להחליף את התמונה מהאדמין בלי דיפלוי.

התמונה בקוד היא רק ברירת מחדל. `usePageCover(pageKey, fallback)` מושך קאבר דינמי
מ-`site_settings` תחת המפתח `cover_image_<pageKey>`, וזה מה שגובר בפועל.

## תבנית של עמוד תוכן חדש

עמוד תוכן חדש (לוח מודעות מסוג כלשהו) הולך לפי הדפוס של `SecondHand.tsx` ו-`RealEstate.tsx`:

- טבלה ב-Supabase עם `is_active`, `is_approved`, `created_by`, ושדות `guest_name` / `guest_email`
- טריגר `<table>_auto_approve` שמאשר אוטומטית חברים מאושרים ואדמינים, ומשאיר אורחים בהמתנה
- טריגר שחוסם שינוי `is_approved` על ידי מי שאינו אדמין
- שני RPC: `get_member_<x>` ו-`get_public_<x>`, כשהציבורי מכבד את `content_access_settings`
- שורה ב-`content_access_settings` עם מפתח העמוד
- הוספת הטיפוס ל-`ContentType` ב-`src/hooks/useContentAccess.ts`
- וריאנט ב-`MembersOnlyNotice.tsx` עם קופי שמתאים לעמוד
- קומפוננטת אדמין עם תור נפרד למודעות שממתינות לאישור
- חיווט: `App.tsx`, `Header.tsx`, `Footer.tsx`, `AdminDashboard.tsx`, `AdminSidebar.tsx`,
  `AdminContentAccess.tsx`, `AdminCovers.tsx`, `scripts/generate-sitemap.ts`, ותרגומים ב-`he` וב-`en`

## טפסים ודיאלוגים

- לא לדחוס שלוש עמודות של שדות. שדה צפוף זה שדה שלא ממלאים.
- הבחירה הראשית בטופס (למשל השכרה מול מכירה) היא כפתורים גדולים, לא דרופדאון.
- לחלק את השדות לקבוצות עם כותרת, לפי הסדר שבו אדם באמת חושב:
  מה מפרסמים, פרטי הנכס, מחיר, יצירת קשר, תמונות.
- גובה שדה נוח הוא `h-11`. placeholder עם דוגמה אמיתית עדיף על שדה ריק.
- תצוגה מקדימה חיה נשארת דביקה (`sticky`) בצד, כדי שרואים מה נבנה תוך כדי מילוי.

## כללי כתיבה בעברית

- לא להשתמש בתו הארוך (—). במקומו פסיק, נקודה או מקף קצר.
- ניסוח בגובה העיניים, בלי שפה שיווקית מנופחת ובלי טון מתנשא.
